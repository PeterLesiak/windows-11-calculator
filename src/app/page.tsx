'use client';

import { type ReactElement } from 'react';

import { Windows11Calculator } from '@/components/Windows11Calculator';

export default function Home(): ReactElement {
  return <Windows11Calculator x={335} y={74} />;
}
